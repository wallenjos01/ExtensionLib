{
  "schemaVersion": 1,
  "id": "${id}-testmod",
  "version": "${version}",
  "entrypoints": {
    "main": [ "org.wallentines.extlib.test.Init" ]
  },
  "depends": {
    "${id}": "${version}",
    "databridge": "*"
  }
}
